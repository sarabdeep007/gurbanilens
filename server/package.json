{
  "name": "gurbanilens-server",
  "version": "0.1.0",
  "private": true,
  "description": "Optional GurbaniLens server — opt-in Whisper-large-v3 fallback ASR + correction feedback queue. DE jurisdiction. No audio storage. No content logging. No user ID. See README.md.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/multipart": "^9.0.0",
    "fastify": "^5.0.0",
    "sql.js": "^1.14.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "vitest": "^4.0.0"
  }
}
